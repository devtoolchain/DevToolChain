import 'package:flutter/widgets.dart';

class TabletPageNotFound extends StatelessWidget {
  const TabletPageNotFound({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}